<div class="p-3 search-results mb-4" data-add-connection-target="list">
  <ul>
    <% users.each do |user| %>
      <li class="search-results"><%= user.username %> <%= link_to "Add", connections_path(receiver_id: user), data: { turbo_method: :post } %></li>
    <% end %>
  </ul>
</div>
